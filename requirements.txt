# Core app
streamlit==1.39.0
pillow==10.4.0

# LangChain split packages (current ecosystem, Oct 2025)
langchain==0.3.7
langchain-core==0.3.15
langchain-community==0.3.5
langchain-openai==0.2.3

# PDF loader dependency
pypdf==5.1.0

# Vector backends
faiss-cpu==1.8.0.post1   # optional but preferred
docarray==0.40.0         # for DocArrayInMemorySearch fallback

# (Optional) if you see SSL / urllib issues on some systems:
requests==2.32.3
